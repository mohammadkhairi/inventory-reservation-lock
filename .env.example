# HTTP server port
PORT=3000

# How often the background expiry sweeper runs (ms).
# Correctness does not depend on this — expired holds are treated as expired
# lazily by effectiveState(). The sweeper only materializes the ACTIVE→EXPIRED
# transition for audit visibility.
SWEEPER_INTERVAL_MS=30000
